---
interface Props {
  variant?: 'link' | 'button';
  urlPath?: string;
  text: string;
  newAttributes?: string;
  id?: string;
}

const { variant = 'link', urlPath, text, newAttributes, id } = Astro.props;
const defaultClass =
  "border border-primaryTeal text-primaryTeal py-2 px-4 rounded-lg inline-block hover:bg-primaryTeal hover:text-white mt-4";
const finalClass = newAttributes
  ? `${defaultClass} ${newAttributes}`
  : defaultClass;
---

{variant === 'link' ? (
  <a href={urlPath} class={finalClass}>
    {text}
  </a>
) : (
  <button id={id} class={finalClass}>
    {text}
  </button>
)}
